import 'package:flutter_test/flutter_test.dart';

void main() {
  // NOTE: Full widget tests will be added per-feature as each phase is implemented.
  // The app requires GetIt and GoRouter to be initialized before testing.
  testWidgets('App smoke test — placeholder', (WidgetTester tester) async {
    // Widget tests will be added starting in Phase 2.
    expect(true, isTrue);
  });
}
